# Credit Scoring Model (PD Prediction)

Проект по разработке модели предсказания вероятности дефолта
(probability of default, PD) на основе исторических данных о заемщиках
(датасет "Give Me Some Credit"). Реализован в виде модульного пайплайна
с фокусом на автоматизацию валидации и бизнес-интерпретацию результатов.

## Цели проекта

- Построение бинарного классификатора для оценки кредитного риска (default / non-default)
- Сравнение базовых и продвинутых моделей:
  - Logistic Regression (baseline)
  - Random Forest
  - Gradient Boosting (CatBoost, LightGBM, XGBoost)
- Оценка качества моделей с использованием:
  - ROC-AUC
  - Gini
- Проверка устойчивости моделей через стратифицированную кросс-валидацию

## Технологический стек

- **Язык**: python
- **ML библиотеки**: scikit-learn, catboost, lightgbm, xgboost
- **Анализ данных**: numpy, pandas, matplotlib, seaborn
- **Инструменты**: EDA, feature engineering

## Структура проекта

Проект организован следующим образом:
- `data/` — исходные и обработанные данные;
- `models/` — сохраненные артефакты (модели, параметры и метрики);
- `notebooks/` — jupyter-ноутбуки с EDA и обучением;
- `src/` — вспомогательные скрипты.

## Результаты моделирования

Проведено сравнение 5 архитектур.
Оценка качества выполнялась на отложенной выборке (hold-out set 20%)
после обучения со стратифицированнной кросс-валидацией (5 fold'ов).

| Модель              | ROC-AUC |  Gini  | Комментарий               |
|---------------------|:-------:|:------:|---------------------------|
| CatBoost            | 0.8703  | 0.7407 | Лучшая стабильность       |
| LightGBM            | 0.8690  | 0.7379 | Высокая скорость обучения |
| XGBoost             | 0.8688  | 0.7376 | Сопоставим с LightGBM     |
| Random Forest       | 0.8630  | 0.7260 | Склонен к переобучению    |
| Logistic Regression | 0.8337  | 0.6675 | Baseline-модель           |

## Ключевые инсайты

- Выявлена сильная нелинейная зависимость между возрастом (`age`) заемщика и риском дефолта: молодые группы имеют самую высокую вероятность дефолта (PD), а старшие — самую низкую.

## Roadmap развития проекта

**Статус проекта: проект находится в стадии разработки!**

Планируемые шаги:
- [ ] Интерпретация факторов: анализ коэффициентов для LogReg и Feature Importance для CatBoost.
- [ ] WoE-бининг: преобразование признаков и расчёт IV для оценки предсказательной силы факторов.
- [ ] Оптимизация порога принятия решений (Threshold) на основе баланса Precision/Recall.

## Как запустить

1. Клонировать репозиторий.
2. Установить зависимости:
```pip install -r requirements.txt```.
3. Последовательно запустить ноутбуки в папке `notebooks/`.